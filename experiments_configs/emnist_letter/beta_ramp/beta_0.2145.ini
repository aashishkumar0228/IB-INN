[checkpoints]
base_name = beta_0.2145_emnist_letter
interval_checkpoint = 200
interval_figure = 200
interval_log = 240

[training]
beta_IB = 0.2145
n_epochs = 60
scheduler_milestones = [50]

[data]
dataset = MNIST
label_smoothing = 0.1
handwriting_type = EMNIST_LETTER

[model]
act_norm = 0.80
